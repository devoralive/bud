define(['project'], function (project) {
    'use strict';

    describe('Project Spec.', function () {
        it('Check project.', function () {
            expect(typeof project).toBe('object');
        });
    });
});
